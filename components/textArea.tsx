"use client";
import { GetFileNameFunction } from "./addNewFiles";

const TextArea: React.FC<{ fileNumber: GetFileNameFunction }> = ({
	fileNumber,
}) => {
	return (
		<div>
			<p>Jel;p</p>
		</div>
	);
};

export default TextArea;
